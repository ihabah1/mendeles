# Railpack (ברירת מחדל) – בלי [build]/Nixpacks.
# migrate + setup_portal רצים ב-scripts/start.sh אחרי עליית הקונטיינר,
# כשהרשת הפרטית (postgres.railway.internal) זמינה.

[deploy]
startCommand = "sh scripts/start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
